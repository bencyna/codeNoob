 {{>nav}}

<div class="dashPage">
    <div class="dash_container">
        <img id="heroImage" src="./images/hero_medium.jpeg"></img>
        <div class="welcomeBack centerScreen">
            <h1>Welcome {{#if logged_in}}back, {{users.first_name}} {{/if}}</h1>
            <h2>It is great to have you here {{#if logged_in}} and contributing! {{/if}}</h2>
            {{#if logged_in}}
            <div class="addModalDiv">
                <button id="addModalBtn"><i data-feather="plus"></i>Add Post</button>
            </div>
            {{/if}}
        </div>
    </div>
    <div class="block"></div>
    <div class="columns is-mobile dashboard">
        <div class="column is-10 is-offset-1">
            <div id="dashboardCon">
                <h1>Discussions and Articles</h1>
                <div class="noobBox">
                    <div class="columns is-one-third">
                        {{#each topics as |topic|}}
                        <div class="postCard topicCard">
                            <h2 class="topicClick" data-id={{this.id}}>{{this.topic_name}}</h2>
                        </div>
                        {{/each}}
                    </div>
                    <div class="columns is-two-thirds">                    
                        {{#each (limit posts 10)}}
                            <div class="postCard">
                                <div class="middle">
                                    <h2 class="postLink" data-id={{this.id}}>{{this.title}}</h2>
                                     <p>{{user.first_name}}</p>
                                </div>
                            </div>
                        {{/each}}
                    </div>
<<<<<<< HEAD
                </div>
            </div>
        </div>
    </div>
    <script src="./js/topics.js"></script>
    <script src="./js/post.js"></script>
</div>
=======
                    {{/each}}
                    <script src="./js/topics.js"></script>
                    <script src="./js/post.js"></script>
>>>>>>> 0f5b18c34605477d2ea6eed0b57519db0c3873c6
